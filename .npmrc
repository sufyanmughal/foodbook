# De machine heeft NODE_ENV=production in de omgeving staan. Daardoor laat npm standaard
# alle devDependencies (typescript, vitest, lint) weg, en werken `npm test`/`npm run
# typecheck` niet. Deze regel zet dat voor dit project expliciet uit.
include=dev
production=false
